@model IEnumerable<TrashCollector.Models.PickUpConfirmation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PickUpStatus }) |
            @Html.ActionLink("Details", "Details", new { id=item.PickUpStatus }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PickUpStatus })
        </td>
    </tr>
}

</table>
